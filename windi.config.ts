export default {
  darkMode: 'media',
  plugins: [
    require('@windicss/plugin-scrollbar')
  ],
  shortcuts: {
    'no-scrollbar': "scrollbar scrollbar-thumb-[rgba(0,0,0,0)] scrollbar-track-[rgba(0,0,0,0)]"
  }
}